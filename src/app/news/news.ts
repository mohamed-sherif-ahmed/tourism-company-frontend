export class News {
    title : string ;
    id:number;
    body:string;
    date:string;

constructor (
              title:         string,
              id:    number,
              body:    string,
              date:    string,
             )  {
                this.title       = title
                this.id   = id
                this.body  = body
                this.date  = date
              }}
